import { defineCollection } from "@nuxt/content";

export default defineCollection({
  // Example: add your collection configuration here
  // See https://content.nuxt.com/docs/api/collection for options
});
